version: 1

agents:
  default: codex
  codex:
    enabled: true
    trigger: "@codex"
  claude:
    enabled: true
    trigger: "@claude"

failureRouting:
  codexAuthoredPRs: codex
  claudeAuthoredPRs: claude
  unknownAuthors: ask
