name: Dep check (reusable)

# Daily replacement for Dependabot's flood of version-update PRs. Runs
# `<pm> outdated` and maintains a single issue per repo with the current
# snapshot grouped by patch / minor / major.
#
# Auto-detects package manager via lockfile (pnpm > npm). Skips silently
# if no lockfile is found (no JS project to check).
#
# Smart-update: the issue only re-saves (and only emails the assignee)
# when the bump list changes since the last run. No-change days are silent.

on:
  workflow_call:
    inputs:
      assignee:
        description: 'GitHub username to assign the daily dep-check issue to'
        type: string
        default: fschrhunt
      node-version:
        description: 'Node.js version to use for dependency checks'
        type: string
        default: '22'
    secrets:
      NODUS_BOT_PAT:
        required: true

jobs:
  check:
    runs-on: [self-hosted, server]
    permissions:
      issues: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Detect package manager
        id: pm
        run: |
          if [ -f pnpm-lock.yaml ]; then
            echo "pm=pnpm" >> "$GITHUB_OUTPUT"
          elif [ -f package-lock.json ]; then
            echo "pm=npm" >> "$GITHUB_OUTPUT"
          else
            echo "pm=none" >> "$GITHUB_OUTPUT"
          fi
          echo "Detected: $(cat $GITHUB_OUTPUT | grep pm=)"

      - name: Skip if no JS project
        if: steps.pm.outputs.pm == 'none'
        run: |
          echo "No JS lockfile found — nothing to check."
          echo "{}" > outdated.json

      - uses: pnpm/action-setup@v6
        if: steps.pm.outputs.pm == 'pnpm'
        with:
          dest: ${{ runner.temp }}/setup-pnpm

      - uses: actions/setup-node@v6
        if: steps.pm.outputs.pm != 'none'
        with:
          node-version: ${{ inputs.node-version }}
          cache: ${{ steps.pm.outputs.pm }}

      - name: Install (pnpm)
        if: steps.pm.outputs.pm == 'pnpm'
        run: pnpm install --frozen-lockfile

      - name: Install (npm)
        if: steps.pm.outputs.pm == 'npm'
        run: npm ci

      - name: Run outdated check (pnpm)
        if: steps.pm.outputs.pm == 'pnpm'
        run: |
          set +e
          pnpm outdated --format=json --long > outdated.json 2>/dev/null
          set -e
          [ -s outdated.json ] || echo "{}" > outdated.json

      - name: Run outdated check (npm)
        if: steps.pm.outputs.pm == 'npm'
        run: |
          set +e
          # npm outdated --json normalises to { name: { current, wanted, latest, type? } }.
          # Older npm versions output an array; jq below tolerates both.
          npm outdated --json > outdated-raw.json 2>/dev/null
          set -e
          [ -s outdated-raw.json ] || echo "{}" > outdated-raw.json
          # Normalise npm's "type" field to "dependencyType" so the parser is uniform.
          jq 'to_entries | map(.value.dependencyType = (.value.type // "dependencies") | .value) | from_entries' \
            outdated-raw.json > outdated.json 2>/dev/null || cp outdated-raw.json outdated.json
          rm -f outdated-raw.json

      - uses: actions/github-script@v7
        env:
          ASSIGNEE: ${{ inputs.assignee }}
        with:
          github-token: ${{ secrets.NODUS_BOT_PAT }}
          script: |
            const fs = require('fs');
            const outdated = JSON.parse(fs.readFileSync('outdated.json', 'utf8'));
            const assignee = process.env.ASSIGNEE;

            const bumpKind = (current, latest) => {
              const [a, b] = String(current).split('.').map(Number);
              const [x, y] = String(latest).split('.').map(Number);
              if (a !== x) return 'major';
              if (b !== y) return 'minor';
              return 'patch';
            };

            const groups = { major: [], minor: [], patch: [] };
            for (const [name, info] of Object.entries(outdated)) {
              const current = info.current;
              const latest = info.latest || info.wanted;
              if (!current || !latest || current === latest) continue;
              groups[bumpKind(current, latest)].push({
                name,
                current,
                latest,
                type: info.dependencyType || info.type,
              });
            }
            for (const k of Object.keys(groups)) {
              groups[k].sort((a, b) => a.name.localeCompare(b.name));
            }

            const count = groups.major.length + groups.minor.length + groups.patch.length;

            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'dependencies',
              per_page: 100,
            });
            const existing = issues.find(i =>
              i.title.startsWith('Daily dep check') || i.title.startsWith('Weekly dep check')
            );

            const fingerprint = JSON.stringify({
              major: groups.major.map(i => `${i.name}@${i.current}>${i.latest}`),
              minor: groups.minor.map(i => `${i.name}@${i.current}>${i.latest}`),
              patch: groups.patch.map(i => `${i.name}@${i.current}>${i.latest}`),
            });
            const fpMarker = `<!-- dep-check-fingerprint: ${fingerprint} -->`;

            if (count === 0) {
              if (existing) {
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: existing.number,
                  state: 'closed',
                  state_reason: 'completed',
                });
                console.log(`Closed stale issue #${existing.number} — nothing outdated.`);
              } else {
                console.log('Nothing outdated. No issue opened.');
              }
              return;
            }

            const unchanged = existing && existing.body && existing.body.includes(fpMarker);

            const renderGroup = (label, items) => {
              if (!items.length) return '';
              const lines = items
                .map(i => `- [ ] \`${i.name}\` \`${i.current}\` → \`${i.latest}\`${i.type === 'devDependencies' ? ' _(dev)_' : ''}`)
                .join('\n');
              return `\n### ${label} (${items.length})\n\n${lines}\n`;
            };

            const date = new Date().toISOString().split('T')[0];
            const title = `Daily dep check — ${date}`;
            const summary = [
              groups.major.length && `${groups.major.length} major`,
              groups.minor.length && `${groups.minor.length} minor`,
              groups.patch.length && `${groups.patch.length} patch`,
            ].filter(Boolean).join(' · ');

            const body = [
              fpMarker,
              `## ${count} package${count === 1 ? '' : 's'} have updates available`,
              ``,
              `_${summary}_`,
              renderGroup('Major (review changelog — breaking changes possible)', groups.major),
              renderGroup('Minor', groups.minor),
              renderGroup('Patch', groups.patch),
              ``,
              `---`,
              ``,
              `**How to use this issue:**`,
              `- Comment \`@claude bump <package>\` to have Claude open a focused PR`,
              `- Or run \`pnpm up <package>\` / \`npm update <package>\` locally and PR the result`,
              `- Check boxes as you handle each one — issue auto-closes when nothing is outdated`,
              ``,
              `_Generated by nodus-bot · runs daily 09:00 CT. You only get emailed when the list changes._`,
            ].join('\n');

            if (existing) {
              if (unchanged) {
                console.log(`No change since last run (#${existing.number}) — skipping update to avoid email.`);
                return;
              }
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing.number,
                title,
                body,
                assignees: [assignee],
              });
              console.log(`Updated issue #${existing.number} — bump list changed.`);
            } else {
              const { data: created } = await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['dependencies'],
                assignees: [assignee],
              });
              console.log(`Opened issue #${created.number}`);
            }
