name: Release Please (reusable)

# Reusable wrapper around googleapis/release-please-action. Caller repo
# must provide its own release-please-config.json and
# .release-please-manifest.json at the repo root.

on:
  workflow_call:
    inputs:
      config-file:
        type: string
        default: release-please-config.json
      manifest-file:
        type: string
        default: .release-please-manifest.json

jobs:
  release-please:
    runs-on: [self-hosted, server]
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Confirm release-please config exists
        run: |
          set -euo pipefail
          test -f "${{ inputs.config-file }}"
          test -f "${{ inputs.manifest-file }}"

      - uses: googleapis/release-please-action@v5
        with:
          config-file: ${{ inputs.config-file }}
          manifest-file: ${{ inputs.manifest-file }}
