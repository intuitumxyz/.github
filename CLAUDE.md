# Claude Instructions

Follow `AGENTS.md` in this repository. The same source-of-truth and safety rules apply to Claude:

- Work on a branch.
- Do not push directly to `main`.
- Do not commit secrets.
- Use `.context/` for handoffs.
- Run relevant checks before handing work back.
- Do not deploy or perform external production changes unless Fischer explicitly asks.
